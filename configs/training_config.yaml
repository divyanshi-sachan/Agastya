# Phase 2 training hyperparameters.
seed: 42
output_dir: results/phase2/models
num_train_epochs: 3
per_device_train_batch_size: 8
learning_rate: 2.0e-5
weight_decay: 0.01
logging_steps: 50
save_steps: 500
